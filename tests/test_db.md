## **Instructions**

- Run the SSH tunnel.
- Make sure all the database credentials from `.env.example` are set in `.env`.
- Make sure the necessary requirements are installed from `requirements.txt`.
- Run the following command: `python -m tests.test_db`

---
