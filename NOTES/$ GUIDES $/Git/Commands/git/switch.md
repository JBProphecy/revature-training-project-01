
---

```
git switch <branch_name>
```

This command will update `HEAD` to reference the specified branch.

---

```
git switch -c <branch_name>
```

This command will create a new branch and update `HEAD` to reference that branch.

---
