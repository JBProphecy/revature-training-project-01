
---

```
python -m venv name
```

`name` - what you want the name of your virtual environment folder to be

This command will create a virtual environment.

---

A good name is `venv`.

```
python -m venv venv
```

This command will create a virtual environment called `venv`.

---
