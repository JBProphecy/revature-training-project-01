
---

```
pyenv local version
```

`version` - the version of `Python` that you intend to use in a project.

This command will create a file called `.python-version` with its content being the value of `version`.

---
