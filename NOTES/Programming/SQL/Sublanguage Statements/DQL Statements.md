
---
# SELECT

The `*` symbol is a wildcard, which will select all the columns in a table.
It's recommended to explicitly define the columns you want to select.

``` sql
SELECT *
FROM my_table;

SELECT id, name
FROM my_table;
```

---
