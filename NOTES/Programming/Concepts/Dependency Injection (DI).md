
---

# Dependency Injection (DI)

**`Dependency Injection (DI)`** is a design pattern that enhances the modularity and testability of of software.

In practice, dependencies are injected into classes via constructor methods (constructor injection), setter methods (setter injection), or directly into fields (field injection).

---
