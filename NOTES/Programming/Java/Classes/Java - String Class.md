
---

# Java - String Class Methods

| Method Name           | Description                                              |
| --------------------- | -------------------------------------------------------- |
| `length`              | returns the length of a string                           |
| `charAt`              | returns the character at a specific position in a string |
| `isEmpty`             | returns true if a string is empty                        |
| `equals`              | returns true if two strings are equal (case-sensitive)   |
| `equalsIgnoreCase`    | returns true if two strings are equal (case-insensitive) |
| `compareTo`           |                                                          |
| `compareToIgnoreCase` |                                                          |
| `indexOf`             |                                                          |
| `lastIndexOf`         |                                                          |
| `contains`            |                                                          |
| `startsWith`          |                                                          |
| `endsWith`            |                                                          |
| `substring`           |                                                          |
| `toLowerCase`         | returns the lowercase version of a string                |
| `toUpperCase`         | returns the uppercase version of a string                |
| `trim`                |                                                          |
| `replace`             |                                                          |
| `replaceAll`          |                                                          |
| `replaceFirst`        |                                                          |
| `split`               |                                                          |
| `join`                |                                                          |
| `toCharArray`         | returns an array of characters built from a string       |
| `valueOf`             |                                                          |
| `format`              |                                                          |
| `matches`             |                                                          |
| `regionMatches`       |                                                          |

---

# Sample of the Source Code

``` java
public final class String
	implements
		CharSequence,
		Comparable<String>,
		Constable,
		ConstantDesc,
		java.io.Serializable
{ ... }
```

---
