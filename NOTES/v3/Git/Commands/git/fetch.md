
---

```
git fetch
```

---

```
git fetch --prune
```

This command is useful when a remote branch no longer exists, but you can still see it locally when you run a command like `git branch -r`.

---
