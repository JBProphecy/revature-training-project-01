
---

## **Installing `Python` with `pyenv-win`**

You can install any version of `Python`.

You can determine the latest stable version through this link.
https://www.python.org/downloads/windows/

As of right now, the latest stable version is `3.14.2`.

Run the following command:

``` powershell
pyenv install 3.14.2
```

---
