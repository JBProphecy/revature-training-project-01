
---

``` powershell
./venv/Scripts/Activate
```

This command will activate your virtual environment in `PowerShell` (if it's called `venv`).

---

``` powershell
deactivate
```

This command will deactivate your virtual environment.

---
